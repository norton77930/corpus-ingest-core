# Implementation plan

1. Preserve 026–031 as an explicit, complete, read-only predecessor digest/static boundary; never execute predecessor tests from the Spec032 verifier.
2. TDD each public seam: approval-bound lease, factory-issued in-memory single-process ledger, exact synthetic driver/metadata parser, lifecycle, and closed Docker declaration.
3. Keep production ledger storage and real Docker subprocess adapter absent from this closure; their successor-only declarations are statically guarded while the five Spec032 targets run under a runtime sentinel.
4. Build a source audit that returns the fixed `BLOCKED_CREDENTIAL_SEAM` result when actual pinned bytes/call path cannot prove the official loader.
5. Stop before Code and Architecture reviews and do not run the final verifier.
