# Tasks: Latest Episode Verified Research Report Workflow

- [x] Establish focused RED tests for Core signature, strict zero-write preview, early exact `expected_episode_ref`/`api_cost_ack` rejection, latest pinning, semantic/research gates, source digest, provenance, atomic publish, reuse, and conflict refusal.
- [x] Add immutable models, storage paths, errors, exports, Core runner, package-private 017 pinned helper, deterministic report assembler, and atomic bundle publisher.
- [x] Add metadata-only checkpoint updates and artifact-driven bundle reuse/adoption behavior.
- [x] Add a thin CLI, append one MCP tool as reviewed tool 15, and add the portable `latest-episode-verified-research-report` Skill.
- [x] Add setup-validator exact-15, Skill metadata, and confirmed early-guard checks.
- [x] Add the full Spec Kit package, registry/handoff/docs/governance mapping, and validation guidance.
- [x] Preserve 015/016/017/research public behavior through targeted regression tests.
- [x] Run final full pytest, compileall, and diff check after post-review documentation convergence.

## Post-Review Remediation Tasks

- [x] Add focused RED regressions for coordinated manifest/report tampering, transcript-invalid adoption, current-summary review hashes/spoofs, secret publication refusal, second source verification, containment, checkpoint merge, canonical fixture options, safe serialization, readable evidence, and destination races.
- [x] Require canonical report/Markdown bytes, complete expected manifest, exactly three final files, source revalidation before rename, and race-safe reuse.
- [x] Gate adoption on `validate_transcript`, complete TXT/SRT/JSON outputs, and transcript identity; preserve pinned 017 fallback when invalid.
- [x] Bind deterministic semantic review to `semantic_summary_sha256` and use one inspector in both 018 gate and assembly.
- [x] Add source/public-output credential guards, bounded stage failure metadata, safe checkpoint reader/merge/unique temporary write, fixture option digesting, and readable classified evidence output.
- [x] Add focused RED regressions for immutable source snapshots, mutation between parse/hash and reuse/race comparison, review artifact authenticity, quoted JSON/YAML credential assignments, checkpoint terminal persistence, production stock-lens schema, and same-second 015 collision re-review.
- [x] Extract neutral semantic-review artifact parser/inspector; require fixed mode/boundary, expected check matrix/counts, canonical-or-collision filename ordering, identity, and current summary hash across 015, corpus index, and 018.
- [x] Snapshot each 018 assembly source once; revalidate snapshots before every publisher success; preserve checkpoint digest/version/bundle references through middle merges and common-finalize all confirmed terminal outcomes.
- [x] Re-evaluate canonical semantic-review checks from immutable bytes in the shared neutral evaluator; replace successful hand-authored all-pass fixtures with production-writer fixtures and reject standard case-insensitive Bearer tokens.
- [x] Resolve semantic summaries only from transcript title identity across 015/index/018; cover Alpha/Zulu stale-candidate integration.
- [x] Add per-bundle and per-checkpoint cross-process claims, return-time bundle/source revalidation, same-second review writer claims with JSON-last commit, and late checkpoint downgrade protection.
- [x] Casefold-reject reserved latest selectors at confirmed 016/018 Core and MCP gates before protected work; preserve the 15-tool registry and 015–017 public contracts.
- [x] Correct drift rejection to an explicit approval-boundary zero-write path; the shared terminal finalizer skips its checkpoint while Core/MCP early guards remain unchanged.
- [x] Replace `O_EXCL` claims with process-lifetime OS locks and persistent lockfiles; prove spawned crash/terminate recovery for both real checkpoint and bundle writers without mtime stale-lock deletion.
- [x] Bind each canonical normalized source path into the source digest and preserve selected review-path provenance so path-distinct authentic re-reviews publish a new auditable version instead of conflicting forever.
- [x] Atomically reserve an episode-local monotonic `invocation_generation` under the checkpoint OS claim after approval drift passes; carry it through every confirmed write and use generation-aware locked CAS merge so an interleaved late stale success preserves the higher-generation digest/version/references while retaining bounded history and reading legacy checkpoints without generations.
- [x] Move disclaimer normalization and direct personalized English/Chinese investment-advice detection into low-level report safety policy shared by neutral review and assembly; explicitly allow only attributed, quoted historical transcript references and remove neutral semantic-review dependencies on stock-lens synthesis, providers, and LLM modules.

## Post-Review Assertion Alignment (handoff convergence)

- [x] Align stock-lens input-set mutation and audit-pair half-commit tests with production error contracts; fail-closed stock-lens missing-boundary expectation; harden Windows concurrent claim seed-write race.
- [x] Crash-recovery tests use honest layering: real checkpoint writer resume + pure claim-name recovery (including bundle claim names); remove lineage-stubbed fake publish arm.
- [x] Re-verify with full pytest, compileall, and git diff --check after alignment (handoff evidence, not a new FR expansion).

## Completion Conditions

- [x] Preview is strict zero-write and invalid confirmation cannot reach RSS/environment/provider/writer/child stage.
- [x] `expected_episode_ref` is canonical and exact `api_cost_ack` is enforced.
- [x] Semantic review gate is exact `passed`; research fixed options exclude force/partial/nested semantic/synthesis work.
- [x] `report.json`, `report.md`, and `manifest.json` are source-digest versioned and atomically published/reused/fail-closed.
- [x] Output contains no raw transcript, secret, URI query/fragment, traceback, live market API, or investment advice.
- [x] Registry has exactly 15 reviewed tools with legacy ordering preserved.
- [x] Full repository pytest suite is green after post-review assertion alignment.
