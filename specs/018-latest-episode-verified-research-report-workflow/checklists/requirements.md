# Requirements Checklist: SPEC 018

- [x] User scenarios cover strict zero-write preview, episode-scoped confirmation, semantic review passed gate, fixed safe research, checkpoint/resume, and atomic bundle reuse/conflict behavior.
- [x] Public Core contract defines `expected_episode_ref`, exact `api_cost_ack`, bounded inputs, and immutable result metadata.
- [x] JSON/Markdown/manifest contract defines report version and source digest.
- [x] CLI, MCP fifteenth tool, portable Skill, setup validator, registry, docs, and governance requirements are represented.
- [x] 015/016/017/research compatibility is required.
- [x] Exact `api_cost_ack`, no live market API, no investment advice, and manual cache rebuild are explicit.
- [x] Post-review requirements cover canonical byte/manifest reuse, transcript-gated adoption, current-summary SHA-bound review, source/public-output safety, pre-rename source verification, bounded stage containment, checkpoint merge/resume metadata, fixture option digesting, strict public inputs, readable evidence, and race-safe destination reuse.
- [x] Immutable source snapshot, post-comparison revalidation, authentic shared semantic-review artifact contract, deterministic collision ordering, checkpoint terminal finalization, quoted credential safety, and production stock-lens structured appendix requirements are covered by focused RED-to-GREEN regressions.
- [x] Shared evaluator recomputes semantic review checks/messages/status/counts from current immutable bytes; forged self-consistent all-pass reviews fail closed and passing fixtures use the production writer/evaluator.
- [x] Transcript-title canonical semantic paths reject Alpha/Zulu stale candidates across 015, corpus index, and 018; standard case-insensitive Bearer tokens fail review and assembly safety.
- [x] Per-bundle/episode claims protect reuse return and checkpoint merges; return-time bundle/source verification and old-terminal downgrade tests fail closed.
- [x] Casefold reserved latest selector tests prove 016/018 Core and MCP guards run before protected dependencies, while the reviewed MCP registry remains exactly 15 tools.
- [x] Approval-boundary latest drift is strict zero-write for expected and resolved episodes; the common finalizer explicitly distinguishes it from checkpoint-persisted terminal outcomes.
- [x] Bundle/checkpoint claims are process-lifetime OS locks with persistent nonblocking lockfiles; spawned terminate recovery resumes each real writer without mtime lock eviction.
- [x] Canonical normalized source paths are bound into the digest while manifests retain selected review-path provenance, so a same-content authentic re-review at a new path creates a separate auditable bundle rather than permanently conflicting with an existing version.
- [x] Episode-local monotonic `invocation_generation` reservation occurs under the checkpoint OS claim after drift approval; every confirmed write carries it, a deterministic A=1/B=2 interleaving preserves B's completed bundle after stale A succeeds, and legacy no-generation checkpoints remain readable.
- [x] Neutral semantic review and assembly share `report_safety` direct personalized English/Chinese advice detection without provider/LLM reverse imports. The policy allows only explicitly attributed, quote-delimited historical transcript references, while direct `You should`, `I recommend`, `consider buying`, and `推薦/建議/值得` buy/sell variants fail review or publication.
- [x] Repository-wide verification completed; this checklist records the actual completed commands.
