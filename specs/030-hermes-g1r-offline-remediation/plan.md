# Implementation Plan

Build sealed closed facts, ledger-bound reference, structured static plan, extended rollback assessment, and exact safe evidence. The verifier is later-only: it runs target tests/compile/static checks, `git diff --check --` only on approved tracked paths, and direct scans all approved Spec030 files including untracked files. It excludes Docker/Hermes/network/inference/C6 and does not authorize them.
