# Research: Verified Report Gap Backlog

## Decisions

1. **B-lite** — gaps only via 022 `has_bundle=False`; no 023 per-row suggest.
2. **Wrapper not fork** — call `list_verified_research_report_coverage` so inventory/bundle rules stay single-sourced.
3. **Tool 21** — agent-facing named backlog without teaching filter semantics.

## Rejected

- B-full (N× 019+016 previews) for v1 cost.
- Mega backfill runner.

## Spec Kit

Full flow: constitution (no amendment) → specify → clarify (session 2026-08-05) → plan → checklist → tasks → analyze → implement → converge.
