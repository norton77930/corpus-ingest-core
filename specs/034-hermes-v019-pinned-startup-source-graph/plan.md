# Spec034 Task #82 current plan

**startup/plugin closed; credential_provider BLOCKED; overall BLOCKED**. Rejected roots `2503bef6` and `6b15c7df` are historical non-evidence. H2 remains exactly 20 upstream paths with inventory SHA-256 `90ba45ccf11bbcbf446f7d16904964073e84837a04aaaa0c6f4887d3ea75109d`.

- Revalidate H2's independently literal 20-path/blob/SHA-256/length authority, the H1 digest, exact filesystem, and no-symlink/reparse ancestor state before every static graph projection.
- Publish only same-parent staged, exclusive/fsynced files, directory rename, manifest-final marker, post-publication validation, and an exclusive-write/same-parent-replace/fsynced phase journal. The publisher/recovery completion check uses only an exact journal nonce/phase/bundle/manifest-bound internal validator; public consumers remain blocked for every lock.
- Parse bytes by AST only; module-level conditional/try/with expressions are startup-owned but nested functions/classes/lambdas never are. Prove `run_agent` module-statement execution of `load_hermes_dotenv` before the first `model_tools` import, and the fixed bundled `plugin.yaml` identity through the same `_load_plugin(manifest)` → `_load_directory_module(manifest)` → `Path(manifest.path)` → `__init__.py`/package-spec → `register(PluginContext(manifest, self))` chain; retain all dynamic/user/project/pip/entry-point/external-secret/runtime paths as blocked or non-claim.
- Use a module-private factory-issued bundle→graph→plugin provenance pair with no public injection seam and a fixed source-free receipt; recompute canonical facts before projection.
- Regress one sealed shared C6 named verified-report public assemble/publish workflow in both final acceptance and non-final regression. Before fixture preparation it installs fail-if-called provider/external/market guards; it asserts timestamp provenance, verified-podcast-fact and needs-verification classifications, independent canonical source digest, first publish/reuse/source-drift rejection, no-investment-advice, and not-requested/not-fetched statuses.
- Pre-review detached seal includes trust launcher, bootstrap, final verifier, stdlib-only isolated runner, sentinel, exact tests/config/docs, the dedicated final-safe acceptance suite plus sealed shared C6 helper, reviewer-approved Python capability manifest, and final invocation contract while excluding only manifest/root self-cycle. External root/bootstrap authority validates detached identities. The verifier returns one typed stable-read authority transaction containing root bytes, manifest bytes, parsed records, approved root digest, and manifest digest; execution records retain those approved bytes and do not reread detached authority pathnames while snapshotting. It stable-reads the remaining reviewed/H2/predecessor union once, then exclusive-writes/fsyncs one same-parent execution snapshot. It separately stable-reads each reviewer-approved purelib capability file against the literal capability manifest and writes a capability snapshot. Parent uses a minimal system-variable allowlist, clears `PYTEST_*` and startup-affecting Python variables, then sets only `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`. The child payload binds cwd to its exact no-link execution snapshot root and changes there before sentinel/import/test startup; C6 relative configuration reads therefore remain snapshot-bound even if the original workspace changes. The `-I -S` child repeats pytest ambient clearing, supplies `--noconftest -o addopts=`, executes verified sentinel bytes before pytest import, admits only exact snapshot paths/categories without shadows, and sets `sys.path` to capability snapshot then project snapshot then stdlib. Its in-memory plugin requires the final-verifier literal authority of eight exact C0–C7 node IDs to collect and pass their call reports; collect-only/skips/xfails cannot succeed. Runner/journal/trust tests remain non-final. Parent revalidates current root/manifest/reviewed bytes/capability/H2/predecessor and both snapshots after child completion. Main alone runs the isolated byte-verified verifier after reviews; capability is a local offline prerequisite, not a portable runtime claim.

## H1 Plan

- Revalidate the exact official commit/tree identity and Spec033 frozen-root identities locally.
- Define fixed, independent roots and source predicates for startup ordering, credential/provider boundary, and security-guidance plugin registration/context mutation candidate.
- Read only those explicit claim paths as immutable individual blobs. Do not enumerate a repository, clone, or archive it.
- Resolve only the literal Hermes-owned edges necessary to each named predicate. Preserve every other internal import as `out_of_claim_internal_leaf`; preserve non-required dynamic behavior as `out_of_claim_dynamic_leaf`.
- Fail closed only for a required edge that is wildcard/nonliteral/path-escaping/unresolved; do not inflate a claim closure to ordinary whole-program imports.
- Enforce 96-file, 8-MiB, and 16-level ceilings across the ordered unique union.
- Write a Spec034-owned exact claim-scoped inventory and safe metadata receipt with `whole_program_source_graph_closed=false`; run the one H1-R self-check.
- Stop for H2 human inventory freeze. H2 remains pending: no source publication, analyzer implementation, or approval occurs before it.

## Task #75 repair plan

- Use independent claim complete/verdict/blocked-reasons tuples. Required wildcard, dynamic, `sys.path`, relative/path-escape, owner-missing, and nonliteral proof edges block their individual claim. Nested scopes never supply owner evidence.
- Validate predecessor Spec033 current review-root, reviewed-manifest, and source-manifest digests directly; never invoke its verifier.
- Seal all final pytest targets, sentinel, actual mutable imports, contracts, docs, successor pointers, and final verifier authority. Do not record H4 as PASS until a new review passes.

