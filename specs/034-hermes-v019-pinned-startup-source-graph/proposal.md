# Spec034 Proposal

H3 implemented a static-only expansion from the human-frozen H2 inventory. Authority is official repository `NousResearch/hermes-agent`, commit `b7a05b6b6f509d14f708a2fe7b7c1d3559396ef6`, tree `3ae46c7c1576f9a3450a64729be314ba8e853eac`, and exactly the 20 records independently pinned in the source validator/acquirer. H1 inventory digest is `90ba45ccf11bbcbf446f7d16904964073e84837a04aaaa0c6f4887d3ea75109d`; no extra upstream path is authorized.

H1 performs bounded official-public discovery for three independent, necessary claim-scoped closures only: (A) startup source ordering, (B) credential/provider boundary, and (C) the bundled `plugins/security-guidance` candidate. It follows only literal Hermes-owned edges needed to interpret each claim's precedence, registration, or construction predicate. Other Hermes-owned imports are transparent `out_of_claim_internal_leaf` records, not recursive expansion. Nonliteral/dynamic behavior outside a required predicate is an `out_of_claim_dynamic_leaf`; a claim-required wildcard, path escape, unresolved target, or unresolved predicate blocks that claim fail closed.

The exact H1 inventory remains bounded to 96 files, 8 MiB, and 16 Hermes-owned levels, never imports or executes upstream source, and sets `whole_program_source_graph_closed=false`. Task #71 has produced `SPEC034_H1_INVENTORY_PROPOSED` only because all three required claim closures are finite and unblocked. H2 must independently freeze that exact inventory; `h2_inventory_approved=false` remains unchanged. H1 does not authorize Docker, Hermes, WSL, MCP transport, provider, inference, credential access, or live observation.
## Task #82 v8 final-review repair — current status

**startup/plugin closed; credential_provider BLOCKED; overall BLOCKED**. Rejected roots `2503bef6`, `6b15c7df`, and every pre-Task #82 identity are not approval evidence. H2 stays exactly 20 upstream paths. The isolated child accepts only a regular, no-link/reparse project snapshot as payload cwd and changes into it after snapshot/capability verification and before sentinel, pytest, or product imports; final parent cwd may remain repository root. Thus C6 relative config reads observe snapshot-approved bytes even if all three original workspace configs are replaced after snapshotting. Public receipt projection has no replaceable verifier parameter; private factory issuance recomputes current canonical facts and retains exact-class/weakref/token/immutable provenance pairing. The bundled plugin AST proof requires owner-local `spec_from_file_location(... init_file ...)` → `module_from_spec(spec)` → `spec.loader.exec_module(module)` → `return module` flow and same module register/context use; aliases and alternate receiver/data substitutions fail closed. Publisher/recovery fsyncs the renamed bundle parent before the `bundle_renamed` journal (documented best-effort fallback where unsupported) and clears only the exact nonce-bound durable-journal/bundle-and-staging-metadata-missing retry state, while manifest/unknown states remain blocked. The bootstrap/final verifier remain unrun; after fresh H4 code and architecture reviews PASS, Main alone must use the unique isolated byte-verified command in `contracts/final-invocation.md`.

