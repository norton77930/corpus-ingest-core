# Spec034 Tasks

- [x] Define H1 official source authority, fixed ceilings, three claim closures, plugin candidate, and no-live boundary.
- [x] Task #71: replace the blocked whole-program expansion with exact ordered claim-scoped path/blob inventory, including required edges, out-of-claim leaves, claim verdicts, union counts, and `whole_program_source_graph_closed=false`.
- [x] Task #71 / H1-R: add targeted offline artifact/receipt checks; demonstrate focused RED against v1 whole-program schema, then GREEN.
- [x] Task #71 / H1-R: run the single planned final self-check after all three claim closures are finite and unblocked.
- [x] H2: human froze H1 inventory SHA-256 `90ba45ccf11bbcbf446f7d16904964073e84837a04aaaa0c6f4887d3ea75109d`, exactly 20 files; no upstream expansion is authorized.
- [x] Task #72 / H3 completed: acquired exact frozen bundle and completed vertical TDD slices for static source catalog, graph, fixed-plugin proof, safe receipt, sentinel, and product-only public-seam regression.
- [ ] Required Code and Architecture Reviews against the detached pre-review root.
- [ ] Main runs the unique offline final verifier once after reviews; it was not run by H3.
- [x] Task #73 may begin; Task #72 H3 implementation claims are PASS-current.
- [x] Task #75: historical first repair; its rejected root `2503bef6` is not approval evidence.
- [x] Task #76: historical second bounded repair; its detached root was rejected and is not approval evidence.
- [x] Task #77: bounded F1–F7 RED→GREEN repair is historical; its detached identity was rejected and is not approval evidence.
- [x] Task #78: G1 publication/recovery internal seam, G2 owner-pruned AST, G3 same-manifest plugin proof, G4 `-I -S` pre-pytest sentinel runner, and G5 fresh detached seal completed by focused RED→GREEN; H2 remains exactly 20 upstream paths and no launcher/bootstrap/final verifier was executed.
- [x] Task #79: v5 review findings repaired by focused RED→GREEN: the detached manifest is the sole project-byte authority for a same-parent exclusive/fsynced project snapshot; an exact reviewer-approved local Python capability manifest is copied from verified purelib bytes into a separate snapshot; the `-I -S` child rejects snapshot extras/links/shadows, verifies pre-import/post-import/post-tests, and imports no original purelib; and plugin proof binds same manifest → loaded module → `exec_module(module)` → same module register/context. H2 remains exactly 20 upstream paths.
- [x] Task #80: v6 review repairs completed by J1/J2/J3 focused RED→GREEN. Typed execution records seal the exact reviewed/detached/H2-upstream/Spec033-predecessor union without a manifest self-cycle; child exactness rejects missing/extra/link/category mismatches. The dedicated sentinel-safe C0–C7 final acceptance target replaces runner/journal/trust meta-tests in the final child. Post-child authority revalidation rejects permanent current drift; replace/execute/restore remains distinguishable from permanent drift. H2 remains exactly 20 upstream paths.
- [x] Task #81: v7 final-review repairs completed by K1–K4 focused RED→GREEN. C6 is one sealed shared sentinel-safe synthetic public assemble/publish workflow, reused by non-final regression and asserting timestamp provenance, verified-podcast-fact, needs-verification inference, independent canonical digest, first/reuse/drift behavior, no-investment-advice, and not-requested/not-fetched statuses under pre-fixture provider/external/market guards. The isolated child uses a minimal ambient allowlist, removes all `PYTEST_*` except protocol autoload disablement, supplies `--noconftest -o addopts=`, and requires exact literal C0–C7 passed-call node IDs. Typed authority retains each stable-read root/manifest/approved identity and execution snapshot bytes; post-child permanent drift still fails. H2 remains exactly 20 upstream paths; final launcher/bootstrap/verifier remain unrun.
- [x] Task #82: v8 final-review findings L1–L5 repaired by focused RED→GREEN. The isolated child rejects non-snapshot/reparse cwd payloads, `chdir`s to the exact execution snapshot before imports/tests, and C6 observes snapshot-approved relative config bytes after original-workspace replacement. Public receipt construction has no caller-injected validator; private issuance recomputes current canonical evidence. Plugin AST proof follows exact spec/module/loader/return dataflow. Publication fsyncs the renamed bundle parent before the `bundle_renamed` journal and retry-safely clears only exact journal durable/both-missing state. H2 remains exactly 20 upstream paths; final launcher/bootstrap/verifier remain unrun.
- [x] Task #83: M1/M2 review blockers repaired by focused RED→GREEN. The isolated child stable-reads all project/capability manifest records into approved in-memory maps before importing; a meta-path verified-bytes importer precedes `PathFinder` and executes approved pure-Python project, Pytest, requests/PyYAML-dependency source, and resource data from those maps. PathFinder fallback into either snapshot is denied, while approved native extension records remain filesystem-only and are refused because the final-safe closure imports none. M2 uses fail-closed direct statement-sequence flow proofs for exact spec → module → guard → exec → return and bundled caller module → register/context flow; dead decoys, nested scope, aliases, and split branches cannot satisfy it. H2 remains exactly 20 upstream paths; final launcher/bootstrap/verifier remain unrun.
- [ ] Task #73: fresh Code and Architecture re-reviews of the Task #83 detached root remain required; rejected roots `2503bef6`, `6b15c7df`, and all earlier identities are not approval evidence.
- [ ] Main-only final invocation after both reviews uses the exact unique isolated byte-verified command in `contracts/final-invocation.md`; it remains unrun. It requires the reviewed local capability manifest's exact digest/distribution set and current approved purelib bytes; this is an offline verifier prerequisite, not a portable runtime claim. The sole final child target is the sentinel-safe C0–C7 acceptance suite; all runner/journal/trust meta verification is non-final. Current terminal: **startup/plugin closed; credential_provider BLOCKED; overall BLOCKED**.

