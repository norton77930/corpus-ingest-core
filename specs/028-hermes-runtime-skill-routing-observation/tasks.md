# Tasks

- [x] T024 Add the pinned source manifest and fail-closed closed-enum/frozen-dataclass capability evaluator, including exact top-level/schema/requirement-keyset rejection.
- [x] T025 Add fixed safe evidence (`spec_id`, `terminal_status`, `runtime_target`, `release_tag`, bounded `missing_requirement_count`) plus the thin offline `capability|synthetic` CLI and hostile synthetic/forged-evaluation cases.
- [x] T026 Add focused tests, Spec 028 contracts/checklists, successor pointers, and governed status documentation.

All three tasks exclude a live runtime path. No exact-21 registry, four Skills, Spec 027 mapping, C6 validator, Hermes integration, or configuration was modified.
