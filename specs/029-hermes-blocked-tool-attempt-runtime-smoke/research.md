# Research boundary

Pinned immutable identifiers only; no raw upstream source, example payload, runtime prompt, response, hook payload, arguments, results, episode reference, endpoint, config, or session material is stored. The unverified one-shot seam causes `BLOCKED_RUNTIME_SEAM` rather than inference.
