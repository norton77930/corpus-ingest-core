# Safe evidence

Fixed projections never contain raw prompt, response, hook payload, tool args/results, podcast ID, episode ref, path, endpoint, session ID, configuration, credential, or source body. A `PASS` only has the narrow pre-dispatch policy-block meaning in `spec.md`.
