# Requirements checklist

- [x] v0.19 G0 offline source identity is exact and drift fails closed
- [x] Baseline disposable-overlay and offline controller lease models remain unauthorized
- [x] Source has pre/post capability; plugin direct `register(ctx)` installs only the pre-tool block hook
- [x] Exact-21 snapshot is derived from the unique FastMCP registry
- [x] Deny-only adapter does not bind or dispatch
- [x] G1 read-only receipt is bounded and blocked (`BLOCKED_CONTROL_PLANE`)
- [ ] G2/G3a authorization (not granted)
